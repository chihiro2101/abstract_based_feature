mpeg-7 is intended to provide complementary functionality to the previous mpeg standards , representing information about the content , not the content itself ( '' the bits about the bits '' ) mpeg-7 can be used independently of the other mpeg standards - the description might even be attached to an analog movie in addition , mpeg-7 descriptions could be used to improve the functionality of previous mpeg standards.with these tools , we can build an mpeg-7 description and deploy it according to the requirements document,1 “ a description consists of a description scheme ( structure ) and the set of descriptor values ( instantiations ) that describe the data extraction algorithms are inside the scope of the standard because their standardization isn ’ t required to allow interoperability the mpeg-7 ( iso/iec 15938 ) consists of different parts relation between description and content an mpeg-7 architecture requirement is that description must be separate from the audiovisual content mpeg-7 uses the following tools : descriptor ( d ) : it is a representation of a feature defined syntactically and semantically description definition language ( ddl ) : it is based on xml language used to define the structural relations between descriptors caliph & emir : annotation and retrieval of images based on mpeg-7 ( gpl ) mpeg-7 overview mpeg-7/-21 community portal 